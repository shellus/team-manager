# 使用 PostgreSQL View 投影账号运营主套餐

## 状态

已接受，2026-08-13。

账号运营主套餐需要把个人订阅与多个 Workspace 的活动成员关系压缩为一个可查询称呼，但这些事实各自拥有独立生命周期。系统使用普通 PostgreSQL View `account_operational_summaries` 在读取时投影，不把结果缓存或双写到账号表；这样列表能够在 SQL 中直接筛选，同时避免成员、角色或套餐变化后出现第二份失效状态。代价是查询需要承担关联与优先级计算，后续应通过数据库集成测试和执行计划约束其正确性与性能。
