# 客户席位关系与到期状态采用派生投影

`SeatSlot` 只保存稳定的客户资料、当前邮箱和到期策略，不再重复保存关系 `status` 或远端用户 ID；关系状态从活动 Membership 和待接受 Invitation 派生，到期状态从北京时间业务日期派生。这样可以避免上游关系已经变化而管理页面仍使用旧缓存，也不会用“本地停用”隐藏仍然存在的上游资格；到期自动移除作为独立可选策略保存有限重试终态，成功或最终失败后均保留客户资料。
