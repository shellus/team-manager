# 用户确认的账号删除级联清理本地关联数据

## 状态

已接受，2026-08-21。

账号删除不再因活动 Workspace 关系、凭证、订单或未结束操作而被永久拒绝。系统先展示本地关联资源摘要，用户确认后删除账号全部本地关联数据；账号作为活动 owner 的 Workspace 及其从属资料一并删除，其他 Workspace 只删除该账号的关系。该动作不调用 ChatGPT 删除远程 Workspace，凭证制品在数据库事务提交后清理并报告失败数量。
