# 本地 Workspace 删除必须显式执行

## 状态

已接受，2026-08-17。

账号不再可访问某个 Workspace 只能证明成员关系已移除，不能证明远端 Workspace 已删除，因此关系同步永不自动删除 Workspace。没有其他活动账号关系和未完成任务时，操作员可以显式彻底删除该 Workspace 的本地成员历史、邀请、凭证、客户席位、账单与订阅快照、订单及已结束操作历史；该操作不调用远端删除接口。
