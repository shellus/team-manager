# 账号内客户资料与到期规则恢复计划

状态：已完成。

## 产品边界

- `SeatSlot` 继续作为 Workspace 下独立的客户运营资料，不并入 Membership 或 Invitation 表。
- 账号详情的 Workspace 成员列表按 `Workspace + 规范化邮箱` 合并成员、邀请和客户资料；没有远端关系的历史客户资料仍作为“未关联”行显示。
- 成员列表展示联系方式、备注、价格和到期日，并通过统一弹窗创建或编辑客户资料。
- 保留释放占用和删除空置客户资料；不恢复复制公开链接、管理员人工换号或独立客户席位页面。
- 到期日是唯一的席位级提醒开关：有到期日就参与到期提醒，没有到期日就不提醒。删除 `expireReminder` 字段和数据库列。
- `expireRemove` 继续表示到期后是否尝试移除远端关系；关闭时到期后只停用本地客户资料。
- 到期提醒日程固定由 `seat_expiration` 策略定义；实际投递使用已启用且配置完整的通知渠道。

## 实施清单

- [x] 删除 `expire_reminder` 数据库列、共享合同和服务分支。
- [x] 为成员、邀请和未关联客户资料建立单表投影。
- [x] 恢复联系方式、备注、价格、到期日和到期处理策略的创建与编辑。
- [x] 恢复释放占用和删除空置资料。
- [x] 修正到期提醒日程与渠道职责。
- [x] 完成迁移、类型检查、测试、构建、真实 API 投影和运行实例验证。
