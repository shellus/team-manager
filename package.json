{
  "name": "team-manager",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "set -a; . ./.env 2>/dev/null || true; set +a; corepack pnpm --parallel --filter @team-manager/web --filter @team-manager/server dev",
    "build": "corepack pnpm --filter @team-manager/shared build && corepack pnpm --filter @team-manager/web build && corepack pnpm --filter @team-manager/server build",
    "typecheck": "corepack pnpm --filter @team-manager/shared build && corepack pnpm -r typecheck",
    "test": "corepack pnpm --filter @team-manager/server test",
    "start": "node apps/server/dist/index.js"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest",
    "vite": "^6.0.0"
  }
}
